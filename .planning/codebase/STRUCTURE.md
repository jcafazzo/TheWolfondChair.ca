# Codebase Structure

**Analysis Date:** 2026-03-04

## Directory Layout

```
TheWolfondChair.ca/
├── .planning/                           # GSD planning documents and phase tracking
│   ├── codebase/                        # Codebase analysis docs (this file, ARCHITECTURE.md)
│   ├── phases/                          # Implementation phase directories
│   │   ├── 01-mobile-fixes/             # Phase 1: Mobile viewport fixes
│   │   └── 02-playwright-tests/         # Phase 2: Playwright test suite
│   └── research/                        # Research and reference docs
├── .claude/                             # Claude Code CLI internals (ignored)
├── images/                              # Static image assets
│   ├── *.jpg, *.png, *.webp             # Team photos, logos, backgrounds
│   └── [23 image files total]
├── tests/                               # Playwright test suite
│   ├── navigation.spec.ts               # Slide navigation tests
│   ├── desktop-regression.spec.ts       # Desktop compatibility tests
│   ├── mobile-viewport.spec.ts          # Mobile touch & viewport tests
│   ├── video-modal.spec.ts              # Video modal functionality tests
│   └── .gitkeep
├── wolfond-report-2024-2026.html        # Single-file application (main delivery)
├── index.html                           # Redirect to wolfond-report-2024-2026.html
├── playwright.config.ts                 # Playwright test configuration
├── package.json                         # Node.js dependencies (@playwright/test)
├── package-lock.json                    # Locked dependency versions
├── CNAME                                # Domain configuration (TheWolfondChair.ca)
└── .gitignore                           # Git ignore rules
```

## Directory Purposes

**`.planning/`:**
- Purpose: GSD (Get Shit Done) framework documentation and phase tracking
- Contains: Architecture/structure analysis, implementation phases, research documents
- Key files: `codebase/ARCHITECTURE.md`, `codebase/STRUCTURE.md` (this file), phase directories with phase-specific plans
- Committed: Yes (markdown files only)

**`.planning/codebase/`:**
- Purpose: Codebase analysis documents generated by `/gsd:map-codebase` command
- Contains: ARCHITECTURE.md, STRUCTURE.md, CONVENTIONS.md, TESTING.md, STACK.md, INTEGRATIONS.md, CONCERNS.md
- Auto-generated for future planning/execution phases

**`.planning/phases/`:**
- Purpose: Store implementation phase definitions and execution logs
- Contains: Individual phase directories (e.g., `01-mobile-fixes/`, `02-playwright-tests/`)
- Each phase has: phase definition markdown, execution summary

**`images/`:**
- Purpose: Static asset storage for presentation content
- Contains: 23 image files (JPGs, PNGs, WebP format)
- Key files: Team member photos, logos (Boehringer Ingelheim, UHN), backgrounds
- Committed: Yes (all tracked in git)

**`tests/`:**
- Purpose: End-to-end test suite using Playwright
- Contains: 4 test spec files (TypeScript) covering navigation, regression, mobile, video modals
- Key files: `navigation.spec.ts` (primary navigation tests), `desktop-regression.spec.ts`, `mobile-viewport.spec.ts`, `video-modal.spec.ts`
- Committed: Yes

**Root directory (project root):**
- Purpose: Application entry points and configuration
- Contains: Application, config files, package management
- Key files: `wolfond-report-2024-2026.html`, `index.html`, `playwright.config.ts`, `package.json`

## Key File Locations

**Entry Points:**
- `wolfond-report-2024-2026.html`: Main presentation application (84 KB, self-contained HTML with embedded CSS and JavaScript)
- `index.html`: Redirect page pointing to main application (for GitHub Pages root)

**Configuration:**
- `package.json`: Node.js project metadata, test script (`playwright test`), @playwright/test dependency
- `playwright.config.ts`: Playwright test runner configuration, browser profiles (desktop-chromium, mobile-iphone14, mobile-pixel5)
- `.gitignore`: Standard Node.js ignore rules

**Core Logic:**
- `wolfond-report-2024-2026.html`: Single file contains:
  - HTML structure (lines 450-486): Deck container, navigation UI template, video modal
  - CSS styles (lines 1-450): Global variables, typography, layout, animations, responsive media queries
  - Data (lines 546-582): `TEAM_PHOTOS` object, image constants, `publications` array
  - JavaScript (lines 585-1350): Slide generation, navigation, event handlers, media orchestration

**Testing:**
- `tests/navigation.spec.ts`: 11 tests for keyboard, click, dot, and swipe navigation
- `tests/desktop-regression.spec.ts`: 7 tests verifying desktop experience not broken by mobile changes
- `tests/mobile-viewport.spec.ts`: 8 tests for touch targets, viewport fitting, scrollable content
- `tests/video-modal.spec.ts`: 5 tests for video modal open/close behavior

**Static Assets:**
- `images/`: Team photos (JPG, PNG formats), brand logos (WebP optimized), backgrounds

## Naming Conventions

**Files:**
- Application file: `wolfond-report-2024-2026.html` (descriptive, date-scoped)
- Tests: `*.spec.ts` (Playwright convention, mirrors spec pattern)
- Config: `playwright.config.ts`, `tsconfig.json` (standard tool naming)
- Images: Descriptive names with extensions (e.g., `Henry_wolfond.jpg`, `Boehringer_Ingelheim_Logo.svg.webp`)

**Directories:**
- Phase directories: `NN-feature-name` pattern (e.g., `01-mobile-fixes`, `02-playwright-tests`)
- Standard names: `images/`, `tests/`, `.planning/`

**CSS Classes:**
- Layout/structure: `.deck`, `.slide`, `.slide-inner`
- Variants: `.cover`, `.split`, `.full-image`, `.dark-slide`, `.deep-slide`, `.letter-slide`, `.grid-slide`
- State: `.active`, `.exiting`
- Components: `.slide-nav`, `.slide-dot`, `.slide-arrow`, `.slide-counter`, `.topbar`, `.video-modal`
- Content: `.split-text`, `.split-image`, `.team-grid-wrap`, `.convo-grid`, `.scroll-content`, `.pub-filter`, `.progress-bar`
- Typography: `.t-display`, `.t-heading`, `.t-subheading`, `.t-body`, `.t-caption`, `.t-italic`

**JavaScript Functions:**
- Navigation: `goToSlide()`, `nextSlide()`, `prevSlide()`, `updateCounter()`
- Initialization: `init()`, `buildSlides()`, `buildPubList()`
- Media: `openVideoModal()`, `closeVideoModal()`, `setupConvoAutoplay()`, `startConvoAutoplay()`, `stopConvoAutoplay()`, `toggleVideoMute()`
- Utilities: `getSlideTheme()`, `applyNavTheme()`, `teamAvatar()`, `startAutoAdvance()`, `stopAutoAdvance()`, `resetAutoAdvance()`, `updateCounterDisplay()`

**Global Variables:**
- State: `current`, `isTransitioning`, `slides`, `autoAdvanceTimeout`, `convoAutoplayInterval`
- Constants: `TEAM_PHOTOS`, `CONVO_SLIDE_INDEX`, `UHN_IMG`, `CAMPUS_IMG`, `publications`

## Where to Add New Code

**New Slide Template:**
- Primary code: Add slide builder function to `wolfond-report-2024-2026.html` (before `buildSlides()` function, ~line 585)
- Pattern: Function returning HTML string with class name matching variant (e.g., `buildSlide_CustomType()`)
- Update: Add function call to `buildSlides()` array at appropriate position
- Tests: Add Playwright spec to `tests/` directory following existing pattern (beforeEach navigates to slide, test assertions)
- CSS: Add variant classes and responsive overrides in `<style>` block (lines 150-400)

**New Data Content:**
- Structured data (publications, team members): Add to constants section (lines 546-582)
  - Publications: Add to `publications` array with `{ year, cite, doi }` structure
  - Team photos: Add to `TEAM_PHOTOS` object with name key
- Background images: Add to `UHN_IMG`, `CAMPUS_IMG` constants or inline in slide generator

**New Navigation Feature:**
- Event handler: Add to document event listener section (keyboard/click/touch, lines 1300-1337)
- State mutation: Update `current` variable or transition flag as needed
- Side effect trigger: Add callback to `goToSlide()` function if state-dependent

**New Test:**
- Location: Create `tests/feature-name.spec.ts`
- Pattern: `test.describe()` suite with `beforeEach()` navigation, multiple test cases with page interactions
- Helpers: Define helper functions (e.g., `counterText()`) within describe block
- Assertions: Use Playwright `expect()` API for DOM queries, attributes, classes, computed styles

**Utilities/Helpers:**
- Shared functions: Add to global scope in JavaScript section (lines 1100-1240)
- Typography scale: Add to CSS variables in `:root` (lines 12-25)
- Color palette: Extend `--color-*` variables in `:root`

## Special Directories

**`.planning/`:**
- Purpose: GSD framework scaffolding and documentation
- Generated: Partially (codebase analysis auto-generated)
- Committed: Yes

**`.claude/`:**
- Purpose: Claude Code CLI cache and plugin internals
- Generated: Yes (by Claude Code framework)
- Committed: No (should be .gitignored, contains node_modules)

**`.git/`:**
- Purpose: Git repository history and refs
- Generated: Yes (by git)
- Committed: N/A (version control metadata)

---

*Structure analysis: 2026-03-04*
